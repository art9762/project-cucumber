# Project cucumber

Платформа для отслеживания новых идей и проектов в сфере ИИ и IT: собирает данные
из публичных источников в единую базу и строит поверх неё анализ.

Проект состоит из двух частей с чётким разделением: **сбор** данных и **анализ**
поверх собранной базы.

## Структура

| Часть | Что это | Статус |
|-------|---------|--------|
| [`search engine/`](search%20engine/) | Ядро сбора (`find-engine`): парсит GitHub, Reddit, Hacker News, arXiv в единую базу PostgreSQL. HTTP API + cron, инкрементальный добор и дедуп. | v1 реализован |
| [`test ui/`](test%20ui/) | Анализ и интерфейс поверх собранной базы. | в работе |

## Архитектура в двух словах

```
источники (GitHub, Reddit, HN, arXiv)
        │
        ▼
  search engine  ──►  PostgreSQL (items + raw_records + jobs)
                            │
                            ▼
                        test ui  (анализ, поиск, дашборды)
```

Ядро **только собирает** данные. Любая аналитика, поиск конкурентов и UI живут
отдельно и читают готовую базу — это позволяет развивать сбор и анализ
независимо.

## С чего начать

- **Запустить сбор данных** → [`search engine/README.md`](search%20engine/README.md)
- **Понять, как устроено ядро** → [`search engine/docs/`](search%20engine/docs/README.md)
- **Дизайн и план v1** → [`search engine/docs/superpowers/specs/2026-06-04-find-engine-design.md`](search%20engine/docs/superpowers/specs/2026-06-04-find-engine-design.md)

## Планы

- Анализ собранной базы: поиск конкурентов, трендов, дашборды (`test ui`).
- Семантический поиск / эмбеддинги (pgvector — схема ядра к этому готова).
- HTML-парсинг произвольных форумов (v2 источников).
